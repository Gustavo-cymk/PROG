#include <stdio.h>
#include <string.h>
#include ctype.h>

int caso3 (char s1[13],char s2[13]){
    for (int i = 0;)

return 1
}
int main (){
    char str1[13];
    char str2[13];
    scanf(" %[^\n]", str1);
    scanf(" %[^\n]", str2);
    cmp = strcmp(str1,str2);
    if (cmp != 0)
        printf("Senha invalida");
    else if (cmp == 0)
        if (sizeof(str1) < 6)
            printf("Senha invalida")
        else if (sizeof(str1 >= 6 ))


return 0;
}
